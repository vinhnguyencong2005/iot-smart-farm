export enum PumpCondition {
  LESS_THAN = 'LESS_THAN',
  GREATER_THAN = 'GREATER_THAN',
}

export enum TriggerSource {
  MANUAL = 'MANUAL',
  ENV = 'ENV',
}

export enum PumpStatus {
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
}
