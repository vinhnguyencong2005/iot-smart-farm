export enum SensorType {
  SOIL_MOISTURE = 'soil',
  TEMPERATURE = 'temp',
  HUMIDITY = 'humid',
  LIGHT_LEVEL = 'light',
}
export enum TriggerCondition {
  LESS_THAN = 'LESS_THAN',
  GREATER_THAN = 'GREATER_THAN',
  EQUAL = 'EQUAL',
}